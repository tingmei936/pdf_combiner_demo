name: pdf_combiner_demo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  file_picker: ^8.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  image_picker: ^1.1.2
  open_app_file: ^4.0.2
  path_provider: ^2.1.5
  pdf_combiner: ^2.1.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
